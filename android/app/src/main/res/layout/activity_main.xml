<?xml version="1.0" encoding="utf-8"?>
<!--
  Root layout: FrameLayout for fragments + BottomNavigationView (Material3) at bottom.

  The single FlutterFragment and all native fragments live in fragment_container.
  Only one fragment is visible at a time (show/hide); the others are hidden but
  their instances are preserved — ensuring no state loss on tab switches.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/nav_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:menu="@menu/bottom_nav_menu" />

</LinearLayout>
